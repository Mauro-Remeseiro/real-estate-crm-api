# Copy this file to .env for local Docker Compose (do not commit .env):
#   cp .env.example .env
#
# Development only — not for production.

# Official SQL Server image variable (maps to the "sa" account).
# Must meet policy: 8+ characters, upper, lower, digit, and symbol.
MSSQL_SA_PASSWORD=CHANGE_ME_StrongPassword123!
